# Medication Inventory API

Stack: Node.js, Express, TypeScript, PostgreSQL, Prisma, Zod, Docker, Vitest, Supertest.

## Running with Docker

```bash
cp .env.example .env
docker compose up --build
